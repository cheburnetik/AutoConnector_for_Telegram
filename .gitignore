# Build outputs
build/
.gradle/
.kotlin/
captures/

# Local config (may contain machine-specific SDK paths)
local.properties

# Signing material — NEVER commit private keys
*.keystore
*.jks
keystore.properties
signing.properties

# Packaged apps (distributed via GitHub Releases, not git)
*.apk
*.aab
*.idsig
*.exe
*.msi
*.sfx

# Desktop packaging artifacts & local test helpers (not part of the source)
dist/
libs-win/
docs/desktop-shots/
run-windows-wine.sh

# IDE / OS
.idea/
*.iml
.DS_Store

# JVM crash artifacts
*.hprof
hs_err_pid*.log
